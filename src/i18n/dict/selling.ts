/** Dropshipping & Selling section: public pages, auth and seller portal. */
export const en = {
  'sellingx.placeholder': '',
} as const;

export const ar: Partial<Record<keyof typeof en, string>> = {
  'sellingx.placeholder': '',
};
