/** Shop storefront flows: catalog, search, product, cart, checkout, account, auth. */
export const en = {
  'shopx.placeholder': '',
} as const;

export const ar: Partial<Record<keyof typeof en, string>> = {
  'shopx.placeholder': '',
};
